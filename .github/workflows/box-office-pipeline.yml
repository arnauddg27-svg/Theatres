name: Box Office Pipeline

on:
  schedule:
    # ── Phase 1: collect showtime links ─────────────────────────────────────
    # Mon-Fri at ~5pm local; Sat-Sun at ~noon local
    # ET 5pm Mon-Fri = 21 UTC (EDT=UTC-4, days 1,2,3,4,5)
    - cron: '0 21 * * 1,2,3,4,5'
    # ET noon Sat/Sun = 16 UTC (days 6,0)
    - cron: '0 16 * * 6,0'
    # CT 5pm Mon-Fri = 22 UTC (CDT=UTC-5, days 1,2,3,4,5)
    - cron: '0 22 * * 1,2,3,4,5'
    # CT noon Sat/Sun = 17 UTC (days 6,0)
    - cron: '0 17 * * 6,0'
    # MT 5pm Mon-Fri = 23 UTC (MDT=UTC-6, days 1,2,3,4,5)
    - cron: '0 23 * * 1,2,3,4,5'
    # MT noon Sat/Sun = 18 UTC (days 6,0)
    - cron: '0 18 * * 6,0'
    # PT 5pm Mon-Fri = midnight UTC next day (PDT=UTC-7, days 2,3,4,5,6)
    - cron: '0 0 * * 2,3,4,5,6'
    # PT noon Sat/Sun = 19 UTC (days 6,0)
    - cron: '0 19 * * 6,0'
    # ── Phase 2: fetch seat maps at ~11pm local every day ───────────────────
    # ET ~11pm = 3am UTC (EDT=UTC-4, every day)
    - cron: '0 3 * * *'
    # CT ~11pm = 4am UTC (CDT=UTC-5, every day)
    - cron: '0 4 * * *'
    # MT ~11pm = 5am UTC (MDT=UTC-6, every day)
    - cron: '0 5 * * *'
    # PT ~11pm = 6am UTC (PDT=UTC-7, every day)
    - cron: '0 6 * * *'
    # Tuesday 10am ET = 2pm UTC — auto-calibrate after weekend numbers are public
    - cron: '0 14 * * 2'
  workflow_dispatch:
    inputs:
      tz_group:
        description: 'Timezone group to scrape (ET, CT, PT, or ALL)'
        required: false
        default: 'ALL'
      phase:
        description: 'Phase to run: collect-links (Phase 1) or scrape (Phase 2)'
        required: false
        default: 'scrape'
      force:
        description: 'Force Phase 2 even if showtime-links.json is stale (>12h old)'
        required: false
        default: 'false'
      test:
        description: 'Test mode: run Phase 2 on N theatres only (e.g. 5). Empty = full run.'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  collect:
    runs-on: [self-hosted, vps]
    timeout-minutes: 90

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          clean: false   # preserve .env and other untracked files between runs

      - name: Install dependencies
        working-directory: box-office-tracker
        run: |
          pip3 install -r requirements.txt
          python3 -m playwright install chromium --with-deps || python3 -m playwright install chromium

      - name: Determine timezone group and phase
        id: tz
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "group=${{ github.event.inputs.tz_group || 'ALL' }}" >> "$GITHUB_OUTPUT"
            echo "phase=${{ github.event.inputs.phase || 'scrape' }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            case "${{ github.event.schedule }}" in
              '0 21 * * 1,2,3,4,5'|'0 16 * * 6,0')
                echo "group=ET" >> "$GITHUB_OUTPUT"
                echo "phase=collect-links" >> "$GITHUB_OUTPUT"
                ;;
              '0 22 * * 1,2,3,4,5'|'0 17 * * 6,0')
                echo "group=CT" >> "$GITHUB_OUTPUT"
                echo "phase=collect-links" >> "$GITHUB_OUTPUT"
                ;;
              '0 23 * * 1,2,3,4,5'|'0 18 * * 6,0')
                echo "group=MT" >> "$GITHUB_OUTPUT"
                echo "phase=collect-links" >> "$GITHUB_OUTPUT"
                ;;
              '0 0 * * 2,3,4,5,6'|'0 19 * * 6,0')
                echo "group=PT" >> "$GITHUB_OUTPUT"
                echo "phase=collect-links" >> "$GITHUB_OUTPUT"
                ;;
              '0 3 * * *')
                echo "group=ET" >> "$GITHUB_OUTPUT"
                echo "phase=scrape" >> "$GITHUB_OUTPUT"
                ;;
              '0 4 * * *')
                echo "group=CT" >> "$GITHUB_OUTPUT"
                echo "phase=scrape" >> "$GITHUB_OUTPUT"
                ;;
              '0 5 * * *')
                echo "group=MT" >> "$GITHUB_OUTPUT"
                echo "phase=scrape" >> "$GITHUB_OUTPUT"
                ;;
              '0 6 * * *')
                echo "group=PT" >> "$GITHUB_OUTPUT"
                echo "phase=scrape" >> "$GITHUB_OUTPUT"
                ;;
              '0 14 * * 2')
                echo "group=ALL" >> "$GITHUB_OUTPUT"
                echo "phase=calibrate" >> "$GITHUB_OUTPUT"
                ;;
              *)
                echo "Unexpected schedule: ${{ github.event.schedule }}" >&2
                exit 1
                ;;
            esac
          else
            echo "Unsupported event type: ${{ github.event_name }}" >&2
            exit 1
          fi

      - name: Auto-calibrate (Tuesday 10am ET only)
        if: steps.tz.outputs.phase == 'calibrate'
        working-directory: box-office-tracker
        run: python3 calibrate.py

      - name: Phase 1 — collect showtime links (${{ steps.tz.outputs.group }})
        if: steps.tz.outputs.phase == 'collect-links'
        working-directory: box-office-tracker
        run: python3 scraper.py --collect-links ${{ steps.tz.outputs.group }} || python3 scraper.py --collect-links ${{ steps.tz.outputs.group }}

      - name: Phase 2 — collect seat data (${{ steps.tz.outputs.group }})
        if: steps.tz.outputs.phase == 'scrape'
        working-directory: box-office-tracker
        run: |
          FORCE_FLAG=""
          if [ "${{ github.event.inputs.force }}" = "true" ]; then FORCE_FLAG="--force"; fi
          TEST_FLAG=""
          if [ -n "${{ github.event.inputs.test }}" ]; then TEST_FLAG="--test ${{ github.event.inputs.test }}"; fi
          python3 scraper.py $FORCE_FLAG $TEST_FLAG ${{ steps.tz.outputs.group }} || python3 scraper.py $FORCE_FLAG $TEST_FLAG ${{ steps.tz.outputs.group }}

      - name: Generate predictions
        if: steps.tz.outputs.phase == 'scrape'
        working-directory: box-office-tracker
        run: python3 predict.py

      - name: Trade on Polymarket bracket markets (Thu-Sun only)
        if: |
          steps.tz.outputs.phase == 'scrape' &&
          (steps.tz.outputs.group == 'PT' || github.event_name == 'workflow_dispatch')
        working-directory: box-office-tracker
        run: |
          # Only trade Thu-Sun (opening weekend). PT runs last so we have all-timezone data.
          # Credentials loaded from /home/gha/.polymarket.env (DRY_RUN=true by default).
          # Change to DRY_RUN=false in that file when ready to go live.
          DOW=$(date +%u)  # 1=Mon ... 7=Sun
          if [ "$DOW" -ge 4 ] || [ "${{ github.event.inputs.force }}" = "true" ]; then
            echo "Day=$DOW — running trade.py..."
            python3 trade.py
          else
            echo "Day=$DOW — skipping trade (only runs Thu=4 through Sun=7)"
          fi

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add box-office-tracker/
          git diff --staged --quiet && exit 0
          git commit -m "data: box office ${{ steps.tz.outputs.group }} ${{ steps.tz.outputs.phase }}"
          for i in 1 2 3; do
            git pull --rebase origin main && git push && break
            echo "Push attempt $i failed, retrying in 10s..."
            [ "$i" = "3" ] && exit 1
            sleep 10
          done
